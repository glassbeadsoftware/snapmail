{
  "name": "Snapmail",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/glassbeadsoftware/snapmail.git"
  },
  "workspaces": [
    "webcomponents",
    "webapp",
    "electron"
  ],
  "scripts": {
    "version"             : "hc --version && holochain --version && lair-keystore --version",
    "ci:hc-install"       : "bash scripts/npm/install-hc-tools.sh",
    "install:hash-zome"   : "bash scripts/npm/install-hash-zome.sh",
    "setup"               : "bash scripts/npm/setup.sh",
    "build:happ"          : "bash scripts/npm/dist-dna.sh",
    "hash-zome"           : "bash scripts/npm/hash-zome.sh",
    "build:webcomponents" : "npm run build -w @snapmail/elements",
    "build:webapp"        : "npm run build -w snapmail",
    "package"             : "npm run build -w @snapmail/elements && npm run package -w snapmail",

    "devtest"             : "npm run clean:hc & npm run build:webcomponents && npm run build:webapp && npm run start:agent",
    "devtest:electron"    : "npm run clean:hc & npm run build:webcomponents && npm run package -w snapmail && npm run electron -w Snapmail",
    "devtest:we"          : "npm run clean:hc & npm run build:webcomponents && npm run build:webapp && npm run devtest -w we-applet",

    "start"               : "npm run build:happ && npm run start:agent",
    "start:agent"         : "DEV_MODE='dev' HC_PORT=$(port) ADMIN_PORT=$(port) concurrently \"npm run build:webcomponents\" \"npm run start:happ-proxied\" \"sleep 10 && npm run start -w snapmail\"",
    "start:happ"          : "RUST_LOG=warn echo \"pass\" | hc s --piped -f=$ADMIN_PORT generate submodules/snapmail.happ --run=$HC_PORT -a snapmail network mdns",
    "start:happ-proxied"  : "RUST_LOG=warn echo \"pass\" | hc s --piped -f=$ADMIN_PORT generate submodules/snapmail.happ --run=$HC_PORT -a snapmail network --bootstrap https://bootstrap-staging.holo.host/ quic -p=kitsune-proxy://SYVd4CF3BdJ4DS7KwLLgeU3_DbHoZ34Y-qroZ79DOs8/kitsune-quic/h/165.22.32.11/p/5779/--",
    "network"             : "npm run build:happ && concurrently-repeat \"npm run start:agent\"",
    "dist:webhapp"        : "rm -f ui.zip && cd electron-ui && bestzip ../webapp/webhapp.workdir/ui.zip * && cd .. && hc web-app pack webapp/webhapp.workdir",
    "dist:win"            : "npm run dist:win -w Snapmail",
    "clean"               : "bash scripts/npm/clean.sh",
    "clean:npm"           : "bash scripts/npm/clean-npm.sh",
    "clean:hc"            : "rm -rf .hc*"
  },
  "devDependencies": {
    "@holochain-playground/cli": "^0.0.11",
    "bestzip": "^2.2.0",
    "concurrently": "^6.2.1",
    "concurrently-repeat": "^0.0.1",
    "cross-env": "^7.0.3",
    "new-port-cli": "^1.0.0",
    "run-singleton-cli": "^0.0.5"
  },
  "engines": {
    "npm": ">=7.0.0"
  }
}
